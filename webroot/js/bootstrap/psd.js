$( document ).ready(function() {

	//======================
	//   Menu Header
	//======================
    $('a').on('click', function(e) {
    	var listElement = $(this);
    	e.preventDefault();
    });

    //$('.collapse').collapse();
});